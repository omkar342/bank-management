<%- include("partials/header")%>
    <div class="customers">
        <h1>These All Are The Proud Users of Our Company.</h1>

        <div>
            <table class="customer-table">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Balance</th>
                    <th class="t-center">Transfer</th>
                </tr>
                <% rows.forEach(function(row,index) { %>
                    <tr>
                      <td><%= ++index %></td>
                      <td><%= row.name %></td>
                      <td><%= row.email %></td>
                      <td>₹ <%= row.balance %></td>
                      <td class="t-center"><a href="/user-details/<%= row.id %>"><button type="button">Transact</button></a></td>
                    </tr>
                    <% }) %>
            </table>
        </div>
    </div>
<%- include("partials/footer")%>   